#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define endl '\n'
#define fastio() ios::sync_with_stdio(false); cin.tie(NULL)

const int MAXN = 2e5 + 5;
ll a[MAXN];

void solve() {
    int n;
    cin >> n;

    for(int i = 0; i < n; i++) {
        cin >> a[i];
    }

    
}

int main() {
    fastio();

    int t = 1;
    cin >> t;

    while(t--) {
        solve();
    }

    return 0;
}